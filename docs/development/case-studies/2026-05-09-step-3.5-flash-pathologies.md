# 2026-05-09 — step-3.5-flash multi-turn  +  bug  nexau 

**TL;DR**:  step-3.5-flash  agent_creator  trace
(`42bce5e2baaf6eaf1528346392aa6062`)  100% 。
bug：(a)  aggregator  step  `reasoning` ；(b) 
`build()`  reasoning-only  `RuntimeError`；(c) **step-3.5-flash
** 3+  prior tool turn  `reasoning`  XML
 tool call， OpenAI  `tool_calls`。Bisect  8 
， prompt-engineering workaround（anti-XML system prompt、
`tool_choice=required`、`tool_choice={specific}`、`temperature=0`）。
PR #542  (a)+(b)；(c)  nexau ， step 。

**Date**: 2026-05-09
**Driver**:  trace  agent_creator  trace ，。
**Files**: PR #542 ─ aggregator/model_response  + 6  step35 SSE
fixture（ production XML-in-reasoning ）。

## 

Langfuse trace `42bce5e2baaf6eaf1528346392aa6062`：

- 37  LLM call
- 32 ""，**0  `tool_calls` **（ text content）
- 5  ERROR：`Choice 0 was never aggregated with any content`
- agent_creator 

## （）

### bug (a) ─  `reasoning` （nexau ）

`OpenAIChatCompletionAggregator._extract_reasoning_delta` docstring "
`reasoning` flat — all routed through `_extract_reasoning_delta`"，**
 line 439  `reasoning_content`  `reasoning_details`，
`reasoning`**。docstring 。Step  `reasoning`， chain-of-thought
， trace 。

### bug (b) ─ reasoning-only stream （nexau ）

`_ChoiceAggregator.build()` "content is None and refusal is None and
no tool_calls" →  `RuntimeError`。 thinking model 
`finish_reason=length` 、 reasoning-only finish=stop（ bug c）
 vendor  ——  reasoning ，UI 
。

### bug (c) ─ step-3.5-flash （）

**100% **（5/5 trials at temperature=0.2）：
 **3+  prior assistant tool turn** ， OpenAI 
 `tool_calls` ， XML  `reasoning`：

```xml
<function=run_shell_command>
<parameter=command>find /tmp -type d</parameter>
<parameter=description>list dirs</parameter>
</function>
</tool_call>
```

：

- `finish_reason=stop`
- `delta.content=""`（ None）
- `delta.tool_calls=[]`
- `usage.completion_tokens_details.reasoning_tokens=0`（vendor 
  ， reasoning token  completion token）

**Bisect **： production trace ：

|  |  role  |  |
|---|---|---|
| sys+user only (2 msgs) | s\|u | ✅  `tool_calls` |
| +last_3 (5 msgs) | s\|u\|t\|a\|t | ✅  `tool_calls` |
| +last_4 (6 msgs) | s\|u\|a\|t\|a\|t | ✅  `tool_calls` |
| +last_5 (7 msgs) | s\|u\|t\|a\|t\|a\|t | ✅  `tool_calls` |
| **+last_6 (8 msgs)** | s\|u\|a\|t\|a\|t\|a\|t（3 prior asst tool ）| ❌ XML in reasoning |
| +last_8 ~ +last_73 | longer | ❌ XML in reasoning |

**Prompt-engineering **：

| Workaround  |  |
|---|---|
|  system prompt  XML /  OpenAI tool_calls | ❌  XML |
| `tool_choice="required"` | ❌  XML |
| `tool_choice={"type":"function","function":{"name":"list_directory"}}`  tool | ❌  XML |
| `temperature=0` | ❌  XML |

 bug (c)  weights ， sampling  prompt 。

## 

PR #542  (a) + (b) +  **executor  vendor-pathology guard**，**
XML parser  (c)**。：

1. ** bug** ——  vendor " OpenAI 
    tool_calls "。 XML ， vendor 。
2. **XML ** —— 、；。
3. **bug (c)  multi-turn agent driver**， nexau 
    XML parse ： context 
   pathology。

### Aggregator-level placeholder substitution（ PR）

 aggregator  `RuntimeError`  —— `ModelResponse` 
`content=None` + `tool_calls=[]` "" history ：

| Vendor |  history  `assistant content=""` / `[]` |
|---|---|
| step-3.5 / OpenAI compat | ✅ ， LLM （recursion）|
| **Anthropic** | ❌ HTTP 400（messages.X.content ）|
| **Gemini** | ❌ HTTP 400（parts ，schema ）|

 `LLMFailoverMiddleware` （ 5xx，""）， session
 turn 、 vendor failover、replay 。

****： `_ChoiceAggregator.build()`  `[empty]`  ——
" stream  message"  aggregator ， middleware。

> ⚠️ ： `EmptyPayloadGuardMiddleware`（ builtin 
> `wrap_model_call` hook）， LLM caller  fork ——
> `if self.middleware_manager:`  sync  vs  async
> ([llm_caller.py:892](../../nexau/archs/main_sub/execution/llm_caller.py)).
>  agent  async  sync ，：(a)  spy 
> async client ；(b) cancellation  thread pool ，
> `test_graceful_stop`  e2e timeout 。：** nexau ，
>  middleware  agent  LLM **。 vendor-pathology
>  aggregator  quirk —— aggregator  LLM call ，
>  middleware 。

```python
# nexau/archs/llm/llm_aggregators/openai_chat_completion/openai_chat_completion_aggregator.py
_EMPTY_CONTENT_PLACEHOLDER = "[empty]"  # 7 chars, ~3 BPE tokens

def build(self) -> ChatCompletionChoice:
    has_any_signal = (content / refusal / tool_calls / reasoning_content / reasoning_details)
    if not has_any_signal:
        raise RuntimeError(...)  # · stream（ reasoning ）→ fail loud
    if (
        not self._value.message.content
        and self._value.message.refusal is None
        and not self._tool_call_aggregators
    ):
        self._value.message.content = _EMPTY_CONTENT_PLACEHOLDER
    ...
```

：

- ** ASCII placeholder `[empty]`** —— BPE  3 token；session resume 
  prompt-cache miss 。 §"prefix cache "。
- **`[...]` ** —— UI ，grep 。
- ** wire ** —— reasoning 
  `message.reasoning_content` （OpenAI/Step serializer  drop，
   Anthropic serializer  ThinkingBlock  + Langfuse trace ）。
- ** fix `openai_chat_completion`** ——  provider（Anthropic/Gemini/
  OpenAI Responses） aggregator ；
  patch（： build()）。

### 

| Sink |  reasoning / raw payload |
|---|---|
| **Langfuse trace** | ✅  —— Langfuse  LLM caller ， aggregator  |
| **`message.reasoning_content`**（in-memory + DB persisted as ReasoningBlock）| ✅  reasoning  |
| **`message.content`**（wire payload to next-turn LLM）| ❌  `[empty]` placeholder（）|

****： Langfuse → ； →  ReasoningBlock 
DB  reasoning（Anthropic  ThinkingBlock ，
OpenAI/Step  —  serializer ）。

（session resume） cache " guard /  append /
 history" ——  turn ：

```
[sys, user, ...prior_real_turns, FAILED_TURN_PLACEHOLDER, new_user]
                                  ↑
                         cache 
```

prefix `[sys, user, ...prior_real_turns]`  100% （ trace 
 49K tokens / 99% cache hit）。`[empty]`  cache miss 
3  token ，。

## （ step  (c)）

|  | ？ |  |
|---|---|---|
|  chat /  / reasoning helper | ✅ | bug (c)  |
|  context（< 5K tokens）+  tool  | ✅ | bug (c)  |
| Multi-turn agent driver（agent_creator / DevBox / code agent ）| ❌ | bug (c) ，agent  |
| Code-editing / file-manipulation agent | ❌ |  long context + multi-turn tool |

##  / 

1. **Aggregator docstring  CI ** —— bug (a)  docstring 
   "reasoning flat" 。： fixture 
    wire shape（recordings/step35_*.sse 
   bare `reasoning`）。
2. **"Choice never aggregated" "" thinking model 
   ** —— thinking model  reasoning  content
   （max_tokens 、、 bug）。 fatal 
    chain-of-thought 。"choice empty" raise
   。
3. **Bisect  LLM **：
   (`sys+user`)  history，。" prompt engineering"
   。
4. **prompt-engineering workaround  bug ** —— 
   workaround **** bug 。，
    bisect ， 4  workaround （system prompt /
   tool_choice / temperature / api param），。
5. **XML-in-reasoning  vendor  nexau ** —— 
    OpenAI ；vendor 、 vendor、
    parser 。，。

## Fixture （PR #542）

 ——  fixture ：

```
tests/aggregator_parity/fixtures/openai_chat/recordings/
  step35_plain.{sse,non_stream.json}                                          # bare-reasoning + content （happy）
  step35_tool_call.{sse,non_stream.json}                                      #  tool_calls （happy）
  step35_reasoning_only_truncated.{sse,non_stream.json}                       # build() reasoning-only （bug b）
  step35_xml_tool_in_reasoning_threshold.{sse,non_stream.json}                # executor _has_empty_payload （bug c）
  step35_workaround_anti_xml_system_prompt_still_fails.{sse,non_stream.json}  #  bug c  nexau 
```

** fixture**  case（： 5 
；）：
- 5/5 trial  →  prompt ， `xml_tool_in_reasoning_threshold`
- bisect （tail=5 OK / tail=7 FAIL）→  threshold + plain
- production 75-msg  →  `xml_tool_in_reasoning_threshold` ，
-  3  workaround（`tool_choice=required` / `tool_choice={specific}` /
  `temperature=0`）→  anti_xml workaround 
- write_file args  / 5  tool schema  8-msg → ，
  case study 
- Negative control（ 8-msg  tool_calls）→ `step35_tool_call`
  "step  tool_calls"

 fixture  stream-vs-non-stream parity， Set A  step  wire
shape  vendor  non-stream build 。

（ `tests/unit/test_openai_chat_completion_aggregator.py::TestReasoningContentStreaming`）：

- `test_bare_reasoning_field_emits_thinking_events` ——  `reasoning`  ThinkingTextMessage events
- `test_bare_reasoning_persisted_into_built_message_reasoning_content` ——  reasoning  `reasoning_content` 
- `test_reasoning_only_truncation_substitutes_placeholder` —— reasoning-only stream → content  `[empty]`， RuntimeError
- `test_xml_tool_call_in_reasoning_aggregates_as_reasoning_only` —— XML-in-reasoning pathology  content=`[empty]` + reasoning_content  XML 

## 

-  → （ P2P，2026-05-09 19:40）：bug (c)  step 
- PR #542：aggregator + fixtures 